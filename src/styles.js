import styled from '@emotion/styled';

import { Section } from 'gatsby-theme-shopify-poulo';

export const ColoredSection = styled(Section)`
  background-color: #1f4940;
  color: white;
`;

export const SectionNoPadding = styled(Section)`
  padding: 0;
`;
export const SectionNoBottomPadding = styled(Section)`
  padding-bottom: 0;
`;
export const SectionNoTopPadding = styled(Section)`
  padding-top: 0;
`;
